{
  "name": "f1-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r exec tsc --noEmit",
    "test": "pnpm -r test",
    "dev:ingest": "pnpm --filter @f1-dash/ingest dev",
    "dev:web": "pnpm --filter @f1-dash/web dev"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.6.3"
  }
}
